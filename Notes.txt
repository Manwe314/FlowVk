wants/needs:
	best support inital setup flexibility. device selection and extentions adding.
	keep buffers modular + possibly have support for 2D images even 
	safety with unique_PTR, size checks asserts and exceptions
	potentially add support for vs code to highlight pipeline / SSBO names when using flow.
	potentially add support for common operations.
	have async dispach with std::future.



flow uogrades to do : 
	support more types of buffers and add image support
	support dynamic kernel entry name
	support custom set of set/binding
	

