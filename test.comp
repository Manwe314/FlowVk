#version 460
layout(local_size_x = 64, local_size_y = 1, local_size_z = 1) in;

@buffer[name="numbers" access="read_only" type="int" layout="std430"]
@buffer[name="outData" access="write_only" type="int" layout="std430"]

void main() {
  uint idx = gl_GlobalInvocationID.x;
  outData.data[idx] = numbers.data[idx] * 2;
}